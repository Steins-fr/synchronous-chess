enum TurnType {
    MOVE_SYNC = 'sync',
    MOVE_INTERMEDIATE = 'intermediate',
    CHOICE_PROMOTION = 'choicePromotion'
}

export enum TurnCategory {
    MOVE = 'move',
    CHOICE = 'choice'
}

export default TurnType;
